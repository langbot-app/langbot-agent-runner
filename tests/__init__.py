"""Contract tests for AgentRunner plugins.

Validates:
- All seven runners are discoverable
- Each runner's manifest satisfies Protocol v1
- Each runner's run() output passes AgentRunResult validation
"""
